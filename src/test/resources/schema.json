{
  "title": "Product",
  "description": "A product from the catalog",
  "type": "object",
  "properties": {
    "id": {
      "description": "The unique identifier for a product",
      "type": "integer",
      "message": "{0} needs to greater or equal to {1}"
    },
    "name": {
      "description": "Name of the product",
      "type": "string",
      "message": "{0} needs to greater or equal to {1}"
    },
    "price": {
      "type": "number",
      "minimum": 0,
      "exclusiveMinimum": true,
      "message": "{0} needs to greater or equal to {1}"
    }
  },
  "required": ["id", "name", "price"]
}